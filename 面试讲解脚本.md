# 面试口头讲解脚本

> 两个版本：⏱ 3分钟版（电梯演讲）和 ⏱ 10分钟版（详细讲解）
> 根据面试官的反应选择用哪个

---

## ⏱ 3分钟版（电梯演讲）

### 开场（30秒）

> 我做了一个**空间数据智能问答 Agent**，用户用自然语言提问，比如"三里屯500米内有什么咖啡店"，系统自动完成意图理解、空间查询、结果格式化，最后返回带地图标注的答案。
>
> 技术栈是 **LangGraph + OSM + DeepSeek**，后端 FastAPI，前端 Streamlit。

### 核心亮点（1.5分钟）—— 挑2-3个说

**亮点1：为什么用 LangGraph？**

> 核心原因是有**条件路由**的需求。意图解析如果失败，我不想让它继续执行空间查询，而是直接跳到结果格式化节点输出错误信息。这个用 DAG 类型的 Chain 是做不到的。
>
> 另外 LangGraph 的 State 机制让每一步的中间结果都落地，调试很方便——哪个节点出了问题一眼就能看到。

**亮点2：意图解析为什么用 LLM，不用规则？**

> 空间查询的自然语言表述**太多样**了。"三里屯附近的咖啡店"和"离三里屯最近的咖啡店"，语义不同，对应不同的工具（buffer_search vs nearest_poi）。
>
> 规则引擎要写大量模板，分类模型要标注数据。LLM 零样本就能理解这些差异，泛化成本最低。

**亮点3：工具层的解耦设计**

> 我把所有空间计算逻辑封装在 `spatial_tools.py`，Agent 的节点只负责调度。这样如果以后要换数据源——比如从 OpenStreetMap 换成高德 API——只需改工具函数，Agent 流程完全不动。

### 收尾（1分钟）

> 目前这个项目能支持5种空间查询意图，26种 POI 类型。如果只是 Demo  level 的项目，我做到这就够了。
>
> 但如果要往生产级做，我会加三件事：
> 1. **RAG 知识库**——让用户问"附近有什么好吃的"这种模糊问题时，能结合评论数据回答
> 2. **多轮对话记忆**——支持"第一家叫什么？离它最近的地铁站呢？"这种追问
> 3. **评估体系**——用 DeepEval 做 Answer Relevance 评估，量化 Agent 质量
>
> 以上就是这个项目的介绍。

---

## ⏱ 10分钟版（详细讲解）

### 第一部分：项目背景（2分钟）

> 我之前做了3年 GIS 和 BIM 的售前解决方案，服务过铁科院、测绘院这类客户。在这个过程中我发现一个问题：
>
> **空间数据的查询门槛很高。** 普通人不知道什么是"POI"、什么是"缓冲区分析"，更不会用 GIS 软件。但他们有很多空间问题想问——"公司附近1公里内有没有便利店""从地铁站到我家的步行距离"。
>
> 大语言模型擅长理解自然语言，osmnx 这种库擅长空间计算，两者结合就能做出低门槛的空间智能问答产品。
>
> 这个项目就是在这个思考下做的。

**面试官可能追问：为什么不用现成的百度地图 AI 对话？**

> 现成的产品是**封闭系统**，它们支持哪些问题、不支持哪些，用户控制不了。我的 Agent 是**开放架构**，工具函数可以任意扩展——今天加一个"查房价"工具，明天加一个"查空气质量"工具，不需要改 Agent 的主体逻辑。这个扩展性在生产环境里很有价值。

---

### 第二部分：架构设计（4分钟）

> 我画一下整体架构。【画在白板上】
>
> 用户 → Streamlit 前端 → FastAPI 后端 → LangGraph Agent → 空间工具层 → OSM 数据

#### LangGraph 工作流（重点讲）

> Agent 的核心是一个 **4节点的 StateGraph**：
>
> 1. **Node 1 意图解析**：把用户输入交给 LLM，输出 JSON，包含 intent 类型、工具名、参数
> 2. **Node 2 参数校验**：检查工具名是否合法，不合法直接标 error
> 3. **Node 3 执行查询**：从工具注册表分发到具体函数，做空间计算
> 4. **Node 4 格式化**：把结构化结果交给 LLM，转成自然语言回复
>
> 条件路由在 Node 1 之后：如果解析失败，直接跳 Node 4 输出错误信息，不浪费资源执行查询。

**面试官可能追问：AgentState 里为什么有 error 字段？不直接抛异常吗？**

> 有两个原因。
> 第一，我想让 Agent **优雅地失败**——抛出错误让用户看到堆栈信息是很糟糕的体验，我更希望 LLM 把错误转成友好的自然语言。
> 第二，error 放在 State 里，可以在 `format_response` 节点里让 LLM 根据错误类型生成不同的回复，而不是千篇一律的"出错了"。

#### 工具层设计

> 工具层有6个函数，收敛在 `TOOL_REGISTRY` 字典里。Agent 通过字符串 key 动态分发，新增工具不需要改 Agent 代码，只要往字典里加一项。
>
> 这里有一个设计细节：**POI 类型映射表**。用户说"咖啡店"，系统自动映射到 OSM 的 `{"amenity": "cafe"}` 标签。目前覆盖了20多种常见类型。这个设计降低了用户的认知负担——不需要知道底层数据模型。

---

### 第三部分：技术决策（2分钟）

#### 为什么用 DeepSeek 而不是 GPT-4o？

> 两个原因：
> 1. **成本**：DeepSeek 比 GPT-4o 便宜20倍以上，做 Demo 和测试时开销小很多
> 2. **中文理解**：这个项目面向中文用户，DeepSeek 的中文语义理解不输 GPT-4o，甚至可能更好
>
> 切换成本很低——LangChain 的 `ChatOpenAI` 类支持自定义 `base_url`，改两行代码就切换了。

#### osmnx 的首次查询延迟问题

> osmnx 第一次查询某个区域时，需要从 OSM 服务器下载全部数据，比较慢（几秒到几十秒）。但它有**本地缓存机制**，第二次查同一个区域是毫秒级。
>
> 如果做生产部署，我会提前用 `ox.features_from_place()` 把热门城市的数据预加载，彻底消除首次延迟。

---

### 第四部分：不足与改进（1.5分钟）

> 这个项目目前还是 Demo 级别，有几个明显的不足：
>
> 1. **没有多轮对话**：每次查询是独立的，不支持"第一家叫什么？离它最近的地铁站呢？"这种追问。解决方法是加 LangGraph 的 `MemorySaver` checkpoint。
> 2. **POI 类型覆盖有限**：目前靠手写映射表，覆盖20多种。更好的方案是接入 RAG，让 LLM 根据用户输入**动态生成** OSM 标签。
> 3. **没有评估体系**：Agent 的质量无法量化。解决方法是引入 DeepEval，建一个测试集，跑 Answer Relevance 和 Faithfulness 指标。
>
> 这些改进方向也是我接下来打算做的。

---

### 收尾（30秒）

> 以上就是整个项目的设计思路和实现细节。
>
> 这个项目让我对 **Agent 的架构设计、工具编排、LLM 与领域系统的结合**有了比较深的理解。这也是为什么我对 Agent 开发岗位特别感兴趣——我觉得这个方向既有技术深度，也有真实的落地价值。

---

## 附：如果被问"现场写一段代码"

### 最可能的题目：写一个 LangGraph 节点

**准备这段模板代码（30秒能写完）：**

```python
from langgraph.graph import StateGraph, END
from typing import TypedDict

class MyState(TypedDict):
    user_input: str
    result: str

def my_node(state: MyState) -> MyState:
    state["result"] = f"处理了: {state['user_input']}"
    return state

workflow = StateGraph(MyState)
workflow.add_node("process", my_node)
workflow.set_entry_point("process")
workflow.add_edge("process", END)
graph = workflow.compile()

result = graph.invoke({"user_input": "hello"})
print(result)
```

> **讲解要点**：State 用 TypedDict 定义，节点是普通函数，conditional edge 用 `add_conditional_edges`。这是 LangGraph 的基本范式，记住这个模板面试现场写代码就不会慌。

---
